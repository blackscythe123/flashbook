import 'package:flutter/material.dart';

/// App color palette inspired by calm, paper-like book reading aesthetics.
/// Colors are derived from the Figma design templates.
class AppColors {
  // Primary brand color - calm blue
  static const Color primary = Color(0xFF1152D4);
  static const Color primaryLight = Color(0xFF3056D3);

  // Background colors - paper-like warm tones
  static const Color backgroundLight = Color(0xFFF6F6F8);
  static const Color backgroundDark = Color(0xFF101622);

  // Surface/Card colors
  static const Color surfaceLight = Color(0xFFFFFFFF);
  static const Color surfaceDark = Color(0xFF1C2433);

  // Paper texture colors (warm off-white)
  static const Color paperLight = Color(0xFFFDFBF7);
  static const Color paperDark = Color(0xFF1A202C);

  // Text colors
  static const Color inkLight = Color(0xFF111318);
  static const Color inkDark = Color(0xFFE7E5E4);
  static const Color textMuted = Color(0xFF616F89);
  static const Color textMutedDark = Color(0xFF9CA3AF);

  // Accent colors
  static const Color accentSage = Color(0xFF84A98C);
  static const Color accentClay = Color(0xFFD4A373);
  static const Color accentGold = Color(0xFFD97706);
  static const Color accentWarm = Color(0xFF8B5E3C);

  // Functional colors
  static const Color success = Color(0xFF22C55E);
  static const Color warning = Color(0xFFF59E0B);
  static const Color error = Color(0xFFEF4444);

  // Gradient colors for decorative elements
  static const Color gradientStart = Color(0xFF3B82F6);
  static const Color gradientEnd = Color(0xFF8B5CF6);
}
